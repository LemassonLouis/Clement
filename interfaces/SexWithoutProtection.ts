/**
 * Represent a sex without protection component.
 */
interface SexWithoutProtectionInterace {
  date: Date,
  sexWithoutProtection: boolean,
  setSexWithoutProtection: React.Dispatch<React.SetStateAction<boolean>>
}